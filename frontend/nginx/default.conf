server {
	listen 3000;
	server_name localhost;

	# HTML 파일이 위치할 루트 경로 설정
	root /usr/share/nginx/html;
	# index.html 파일명 설정
	index index.html index.htm;

	# error_page 500 502 503 504 /50x.html;

	location / {}
}
